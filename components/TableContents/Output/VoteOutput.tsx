import BackToTable from "../../Table/BackToTable";

const VoteOutput = ({ id }) => {
  return (
    <div>
      VoteOutput
      <div>{id}</div>
      <div>
        <BackToTable />
      </div>
    </div>
  );
};

export default VoteOutput;
